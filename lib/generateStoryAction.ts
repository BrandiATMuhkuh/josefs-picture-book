"use server";

export async function generateStoryAction(formData: FormData) {
  "use server";

  // mutate data
  // revalidate cache

  console.log("form data", formData);
  //   redirect("/dashboard");

  return { abc: 1 };
}
